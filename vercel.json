{
  "builds": [
    {
      "src": "index.js", 
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "50mb"
      }
    },
    {
      "src": "package.json",
      "use": "@vercel/node",
      "env": {
        "NODE_ENV": "production"
      }
    }
  ],
  "scripts": {
    "build": "prisma generate && npm run build"
  }
}